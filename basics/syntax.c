#include <stdio.h>           // načíta hlavičkový súbor ktorý slúži na vstup a výstup

int main() {                 // main je hlavná funkcia vykoná všetko v { } týchto zátvorkách { touto začína

    printf("Hello World!");  // printf - slúži na výpis textu píše sa v (" ");

    return 0;                // ukončí funkciu main a vráti hodnotu späť operačnému systému 
}                            // uzatvára funkciu main }