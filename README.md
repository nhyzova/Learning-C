# 💻 Learning C

Toto je môj osobný repozitár, v ktorom sa učím programovací jazyk **C**.
Postupne sem pridávam príklady, vlastné poznámky a menšie programy, ktoré vytváram počas učenia.

# 📂 Obsah projektu

## 📖 Poznámky

- 📄 `C_Notes.md` - stručné poznámky a teória k jednotlivým témam.

---

## 🧱 Základy

- 📄 `syntax.c` - syntax
- 📄 `output.c` - výpis pomocou `printf()`
- 📄 `variables.c` - premenné
- 📄 `data_types.c` - dátové typy
- 📄 `operators.c` - operátory

---

## 🔀 Riadenie programu

- 📄 `if_else.c` - podmienky
- 📄 `break_continue.c` - `break` a `continue`
- 📄 `for_loop.c` - cyklus `for`
- 📄 `while_loop.c` - cyklus `while`
- 📄 `nested_for_loop.c` - vnorené cykly

---

## 📦 Polia

- 📄 `arrays.c` - jednorozmerné polia
- 📄 `array_loop.c` - prechádzanie poľa pomocou cyklov
- 📄 `array_size.c` - operátor `sizeof()`
- 📄 `multidimensional_arrays.c` - viacrozmerné polia

---

## 🔤 Reťazce

- 📄 `strings.c` - základy práce s reťazcami
- 📄 `strings_special.c` – špeciálne znaky
- 📄 `strings_functions.c` – funkcie `strlen()`, `sizeof()`, `strcpy()`, `strcat()` a `strcmp()`

---

## ⌨️ Vstup používateľa

- 📄 `user_input.c` – scanf(), fgets()

---

## 🧠 Pamäť

- 📄 `memory_address.c` – adresy v pamäti
- 📄 `pointer_arithmetic.c` – aritmetika pointerov
- 📄 `pointer_to_pointer.c` – pointer na pointer
- 📄 `pointers.c` – pointery
- 📄 `pointers_arrays.c` – pointery a polia

---

## 🧠 Memory Management

- 📄 `memory_management.c` – základy správy pamäte v C

---

## ⚙️ Funkcie

- 📄 `functions.c` – základy funkcií
- 📄 `function_parameters.c` – parametre funkcií
- 📄 `scope.c` – lokálny a globálny scope
- 📄 `function_declaration` - deklarácia a definícia funkcie
- 📄 `math_functions.c` – matematické funkcie (`math.h`)
- 📄 `inline_function.c` – inline funkcie
- 📄 `recursion.c` - rekurzívne funkcie
- 📄 `function_pointer.c` - ukazovateľ na funkciu
- 📄 `callback_function.c` – callback funkcie (spätné volania)

---

## 📁 Súbory

- 📄 `create_files.c` - vytváranie súborov pomocou `fopen()`
- 📄 `write_to_files.c` - zapisovanie údajov do súborov (`fprintf()`)
- 📄 `read_files.c ` - čítanie údajov zo súborov (`fgets()`, `fscanf()`)

---

## 🏗️ Štruktúry

- 📄 `structures.c` - základy štruktúr (`struct`)
- 📄 `nested_structs.c` - vnorené štruktúry
- 📄 `structs_pointers.c` - ukazovatele na štruktúry
- 📄 `unions.c` - únia (`union`) a zdieľaná pamäť
- 📄 `typedef.c` - aliasy dátových typov (`typedef`)
- 📄 `struct_padding.c` - zarovnanie a padding štruktúr
- 📄 `enums.c` - enumerácie (`enum`)

---

# 📈 Progress

🎯 **Current Progress:** **83%**

📖 **Current Topic:** C Memory Management

📅 Last Updated: **13.08.2026**

---

⭐ Tento repozitár budem priebežne rozširovať o ďalšie témy, cvičenia a menšie projekty.
